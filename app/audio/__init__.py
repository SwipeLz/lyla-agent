"""Audio backend (Phase 10).

Hermetic-only fake STT and TTS. No provider SDKs. No network calls.
See `docs/AUDIO_BACKEND.md` for the runbook and `AGENTS.md` Property AR7
for the import-time hermeticity rule enforced by
`app/tests/test_audio_fake_hermeticity.py`.
"""

__all__: list[str] = []
